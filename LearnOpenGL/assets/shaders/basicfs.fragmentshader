
#version 330 core
out vec4 FragColor;

in vec3 ourColor;   // the input variable from the vertex shader (same name and same type)

// 在某些驱动程序中，需要为每个采样器sampler2D uniform 变量分配一个纹理单元
uniform sampler2D ourTexture;

void main()
{
    FragColor = vec4(ourColor, 1.0f);
} 